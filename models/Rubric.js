// const { Schema, model, Types } = require('mongoose')
// const schema = Schema({
//   title: { type: String, required: true, unique: true },
//   messages: [{ type: Types.ObjectId, ref: "Message" }],
//   authors: [{ type: Types.ObjectId, ref: "User" }],
// })

// module.exports = model("Rubric", schema)